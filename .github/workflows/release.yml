name: Release

on:
  push:
    tags:
      - "*.*.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        with:
          version: 10.30.1

      - uses: actions/setup-node@v6
        with:
          node-version: "24.13.1"
          cache: "pnpm"

      - run: pnpm install

      - run: pnpm test

      - run: pnpm build

      - name: Package skills
        run: |
          cd skills/lobster-init && zip -r ../../lobster-init.skill . && cd ../..
          cd skills/lobster-css  && zip -r ../../lobster-css.skill  . && cd ../..

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/lobster.js
            lobster-init.skill
            lobster-css.skill
